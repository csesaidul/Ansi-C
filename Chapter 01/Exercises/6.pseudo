// Write an algorithm whether a year is a leap year or not.
START
INPUT year
IF year % 4 = 0 AND year % 100 != 0 OR year % 400 = 0 THEN
    OUTPUT "Leap year"
ELSE
    OUTPUT "Not a leap year"
END IF
END